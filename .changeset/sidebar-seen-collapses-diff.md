---
'@super-review/desktop': patch
---

Marking a file as seen from the sidebar now collapses it in the diff view, matching the diff view's own "mark seen" behavior. Unmarking a file leaves its collapsed/expanded state untouched.
