---
'@super-review/desktop': patch
---

feat: rename the bundled skill from `document-session` to `super-review`, with a routing `SKILL.md` that points to `document-session.md` (authoring a tour) and `resolve-comments.md` (reading and resolving inline review comments). "Install skill" now drops `.agents/skills/super-review/` into the repo.
