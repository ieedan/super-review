---
'@super-review/desktop': patch
---

feat: arrange the notices above the commit box into a hover-to-expand stack (like Sonner), each a dismissible card, and add a prompt to install the super-review skill when it isn't installed in the current project.
