---
'@super-review/desktop': patch
---

fix: select the first expanded change visible on the page in the sidebar, so a collapsed file's pinned header no longer claims active while an expanded file below it is the one on screen
