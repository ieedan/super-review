---
"@super-review/desktop": patch
---

Hide the GitHub CI status indicator on the primary action button while a push/fetch/pull is in progress, so it no longer renders alongside the "Working…" state.
