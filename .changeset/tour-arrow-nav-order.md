---
'@super-review/desktop': patch
---

Arrow-key navigation in a session's Tour view now steps through files in tour (reading) order, matching how they appear in the diff and the tour sidebar, instead of jumping around in the underlying file-tree order.
