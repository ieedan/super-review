---
'@super-review/ui': patch
---

fix: the comment editor now renders on a single uniform background. The Write/Preview toolbar previously used a lighter shade than the editor body, and on the Write tab the syntax-highlight overlay painted a darker box behind the typed text. Both now match the editor background.
