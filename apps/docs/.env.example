# Upstash Redis REST credentials for the waitlist endpoint (/api/waitlist).
# Create a database at https://console.upstash.com and copy the REST URL + token.
# On Vercel, set these as Environment Variables for the project.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
