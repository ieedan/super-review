{
	"token": "widget",
	"totalMatches": 9198,
	"fixtures": [
		{
			"file": {
				"path": "src/features/module-00/widget-0.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-00/widget-0.ts bb/src/features/module-00/widget-0.ts\nindex 3977931e..cb07031c 100644\n--- aa/src/features/module-00/widget-0.ts\n+++ bb/src/features/module-00/widget-0.ts\n@@ -8,10 +8,11 @@ export function setupWidget0_0(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 0 file 0: a widget controller.\nexport function setupWidget0_0(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 0 file 0: a widget controller.\nexport function setupWidget0_0(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-00/widget-1.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-00/widget-1.ts bb/src/features/module-00/widget-1.ts\nindex 0742317b..f1da9556 100644\n--- aa/src/features/module-00/widget-1.ts\n+++ bb/src/features/module-00/widget-1.ts\n@@ -8,10 +8,11 @@ export function setupWidget0_1(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 0 file 1: a widget controller.\nexport function setupWidget0_1(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 0 file 1: a widget controller.\nexport function setupWidget0_1(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-00/widget-2.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-00/widget-2.ts bb/src/features/module-00/widget-2.ts\nindex 1491d9ad..dfe30053 100644\n--- aa/src/features/module-00/widget-2.ts\n+++ bb/src/features/module-00/widget-2.ts\n@@ -8,10 +8,11 @@ export function setupWidget0_2(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 0 file 2: a widget controller.\nexport function setupWidget0_2(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 0 file 2: a widget controller.\nexport function setupWidget0_2(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-00/widget-3.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-00/widget-3.ts bb/src/features/module-00/widget-3.ts\nindex 216bbe81..61df3f97 100644\n--- aa/src/features/module-00/widget-3.ts\n+++ bb/src/features/module-00/widget-3.ts\n@@ -8,10 +8,11 @@ export function setupWidget0_3(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 0 file 3: a widget controller.\nexport function setupWidget0_3(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 0 file 3: a widget controller.\nexport function setupWidget0_3(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-00/widget-4.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-00/widget-4.ts bb/src/features/module-00/widget-4.ts\nindex c1b455ae..4d13a331 100644\n--- aa/src/features/module-00/widget-4.ts\n+++ bb/src/features/module-00/widget-4.ts\n@@ -8,10 +8,11 @@ export function setupWidget0_4(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 0 file 4: a widget controller.\nexport function setupWidget0_4(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 0 file 4: a widget controller.\nexport function setupWidget0_4(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-01/widget-0.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-01/widget-0.ts bb/src/features/module-01/widget-0.ts\nindex 90b4be4b..49e47d5b 100644\n--- aa/src/features/module-01/widget-0.ts\n+++ bb/src/features/module-01/widget-0.ts\n@@ -8,10 +8,11 @@ export function setupWidget1_0(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 1 file 0: a widget controller.\nexport function setupWidget1_0(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 1 file 0: a widget controller.\nexport function setupWidget1_0(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-01/widget-1.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-01/widget-1.ts bb/src/features/module-01/widget-1.ts\nindex c412df28..b56f4a32 100644\n--- aa/src/features/module-01/widget-1.ts\n+++ bb/src/features/module-01/widget-1.ts\n@@ -8,10 +8,11 @@ export function setupWidget1_1(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 1 file 1: a widget controller.\nexport function setupWidget1_1(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 1 file 1: a widget controller.\nexport function setupWidget1_1(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-01/widget-2.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-01/widget-2.ts bb/src/features/module-01/widget-2.ts\nindex 9d1f3913..ea36e3a8 100644\n--- aa/src/features/module-01/widget-2.ts\n+++ bb/src/features/module-01/widget-2.ts\n@@ -8,10 +8,11 @@ export function setupWidget1_2(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 1 file 2: a widget controller.\nexport function setupWidget1_2(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 1 file 2: a widget controller.\nexport function setupWidget1_2(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-01/widget-3.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-01/widget-3.ts bb/src/features/module-01/widget-3.ts\nindex 9214c4e9..948893c3 100644\n--- aa/src/features/module-01/widget-3.ts\n+++ bb/src/features/module-01/widget-3.ts\n@@ -8,10 +8,11 @@ export function setupWidget1_3(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 1 file 3: a widget controller.\nexport function setupWidget1_3(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 1 file 3: a widget controller.\nexport function setupWidget1_3(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-01/widget-4.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-01/widget-4.ts bb/src/features/module-01/widget-4.ts\nindex 8ec4d497..f9d5d584 100644\n--- aa/src/features/module-01/widget-4.ts\n+++ bb/src/features/module-01/widget-4.ts\n@@ -8,10 +8,11 @@ export function setupWidget1_4(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 1 file 4: a widget controller.\nexport function setupWidget1_4(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 1 file 4: a widget controller.\nexport function setupWidget1_4(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-02/widget-0.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-02/widget-0.ts bb/src/features/module-02/widget-0.ts\nindex 4d18b05e..6f00bd3a 100644\n--- aa/src/features/module-02/widget-0.ts\n+++ bb/src/features/module-02/widget-0.ts\n@@ -8,10 +8,11 @@ export function setupWidget2_0(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 2 file 0: a widget controller.\nexport function setupWidget2_0(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 2 file 0: a widget controller.\nexport function setupWidget2_0(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-02/widget-1.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-02/widget-1.ts bb/src/features/module-02/widget-1.ts\nindex 4b3ac4bb..85609449 100644\n--- aa/src/features/module-02/widget-1.ts\n+++ bb/src/features/module-02/widget-1.ts\n@@ -8,10 +8,11 @@ export function setupWidget2_1(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 2 file 1: a widget controller.\nexport function setupWidget2_1(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 2 file 1: a widget controller.\nexport function setupWidget2_1(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-02/widget-2.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-02/widget-2.ts bb/src/features/module-02/widget-2.ts\nindex 0008be2e..114e8fb3 100644\n--- aa/src/features/module-02/widget-2.ts\n+++ bb/src/features/module-02/widget-2.ts\n@@ -8,10 +8,11 @@ export function setupWidget2_2(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 2 file 2: a widget controller.\nexport function setupWidget2_2(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 2 file 2: a widget controller.\nexport function setupWidget2_2(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-02/widget-3.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-02/widget-3.ts bb/src/features/module-02/widget-3.ts\nindex eb8849ee..20dab1a7 100644\n--- aa/src/features/module-02/widget-3.ts\n+++ bb/src/features/module-02/widget-3.ts\n@@ -8,10 +8,11 @@ export function setupWidget2_3(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 2 file 3: a widget controller.\nexport function setupWidget2_3(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 2 file 3: a widget controller.\nexport function setupWidget2_3(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-02/widget-4.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-02/widget-4.ts bb/src/features/module-02/widget-4.ts\nindex c0db9d90..54438e4e 100644\n--- aa/src/features/module-02/widget-4.ts\n+++ bb/src/features/module-02/widget-4.ts\n@@ -8,10 +8,11 @@ export function setupWidget2_4(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 2 file 4: a widget controller.\nexport function setupWidget2_4(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 2 file 4: a widget controller.\nexport function setupWidget2_4(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-03/widget-0.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-03/widget-0.ts bb/src/features/module-03/widget-0.ts\nindex c134eeff..783bfa5c 100644\n--- aa/src/features/module-03/widget-0.ts\n+++ bb/src/features/module-03/widget-0.ts\n@@ -8,10 +8,11 @@ export function setupWidget3_0(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 3 file 0: a widget controller.\nexport function setupWidget3_0(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 3 file 0: a widget controller.\nexport function setupWidget3_0(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-03/widget-1.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-03/widget-1.ts bb/src/features/module-03/widget-1.ts\nindex 4295d199..c63dd7ba 100644\n--- aa/src/features/module-03/widget-1.ts\n+++ bb/src/features/module-03/widget-1.ts\n@@ -8,10 +8,11 @@ export function setupWidget3_1(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 3 file 1: a widget controller.\nexport function setupWidget3_1(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 3 file 1: a widget controller.\nexport function setupWidget3_1(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-03/widget-2.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-03/widget-2.ts bb/src/features/module-03/widget-2.ts\nindex 38632265..3b134ae1 100644\n--- aa/src/features/module-03/widget-2.ts\n+++ bb/src/features/module-03/widget-2.ts\n@@ -8,10 +8,11 @@ export function setupWidget3_2(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 3 file 2: a widget controller.\nexport function setupWidget3_2(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 3 file 2: a widget controller.\nexport function setupWidget3_2(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-03/widget-3.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-03/widget-3.ts bb/src/features/module-03/widget-3.ts\nindex 444c58cd..e49c9767 100644\n--- aa/src/features/module-03/widget-3.ts\n+++ bb/src/features/module-03/widget-3.ts\n@@ -8,10 +8,11 @@ export function setupWidget3_3(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 3 file 3: a widget controller.\nexport function setupWidget3_3(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 3 file 3: a widget controller.\nexport function setupWidget3_3(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-03/widget-4.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-03/widget-4.ts bb/src/features/module-03/widget-4.ts\nindex f7c6c0d8..68ef67f1 100644\n--- aa/src/features/module-03/widget-4.ts\n+++ bb/src/features/module-03/widget-4.ts\n@@ -8,10 +8,11 @@ export function setupWidget3_4(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 3 file 4: a widget controller.\nexport function setupWidget3_4(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 3 file 4: a widget controller.\nexport function setupWidget3_4(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-04/widget-0.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-04/widget-0.ts bb/src/features/module-04/widget-0.ts\nindex 323bf40e..5c19c1f3 100644\n--- aa/src/features/module-04/widget-0.ts\n+++ bb/src/features/module-04/widget-0.ts\n@@ -8,10 +8,11 @@ export function setupWidget4_0(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 4 file 0: a widget controller.\nexport function setupWidget4_0(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 4 file 0: a widget controller.\nexport function setupWidget4_0(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-04/widget-1.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-04/widget-1.ts bb/src/features/module-04/widget-1.ts\nindex 8bb9a020..cb1fd9c8 100644\n--- aa/src/features/module-04/widget-1.ts\n+++ bb/src/features/module-04/widget-1.ts\n@@ -8,10 +8,11 @@ export function setupWidget4_1(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 4 file 1: a widget controller.\nexport function setupWidget4_1(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 4 file 1: a widget controller.\nexport function setupWidget4_1(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-04/widget-2.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-04/widget-2.ts bb/src/features/module-04/widget-2.ts\nindex 85c29263..a10d47e6 100644\n--- aa/src/features/module-04/widget-2.ts\n+++ bb/src/features/module-04/widget-2.ts\n@@ -8,10 +8,11 @@ export function setupWidget4_2(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 4 file 2: a widget controller.\nexport function setupWidget4_2(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 4 file 2: a widget controller.\nexport function setupWidget4_2(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-04/widget-3.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-04/widget-3.ts bb/src/features/module-04/widget-3.ts\nindex e3a0bb51..3f3a7ea0 100644\n--- aa/src/features/module-04/widget-3.ts\n+++ bb/src/features/module-04/widget-3.ts\n@@ -8,10 +8,11 @@ export function setupWidget4_3(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 4 file 3: a widget controller.\nexport function setupWidget4_3(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 4 file 3: a widget controller.\nexport function setupWidget4_3(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-04/widget-4.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-04/widget-4.ts bb/src/features/module-04/widget-4.ts\nindex eb834143..fdcbf996 100644\n--- aa/src/features/module-04/widget-4.ts\n+++ bb/src/features/module-04/widget-4.ts\n@@ -8,10 +8,11 @@ export function setupWidget4_4(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 4 file 4: a widget controller.\nexport function setupWidget4_4(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 4 file 4: a widget controller.\nexport function setupWidget4_4(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-05/widget-0.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-05/widget-0.ts bb/src/features/module-05/widget-0.ts\nindex 7dd765d3..7ae86c53 100644\n--- aa/src/features/module-05/widget-0.ts\n+++ bb/src/features/module-05/widget-0.ts\n@@ -8,10 +8,11 @@ export function setupWidget5_0(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 5 file 0: a widget controller.\nexport function setupWidget5_0(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 5 file 0: a widget controller.\nexport function setupWidget5_0(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-05/widget-1.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-05/widget-1.ts bb/src/features/module-05/widget-1.ts\nindex 09656c44..7e44820e 100644\n--- aa/src/features/module-05/widget-1.ts\n+++ bb/src/features/module-05/widget-1.ts\n@@ -8,10 +8,11 @@ export function setupWidget5_1(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 5 file 1: a widget controller.\nexport function setupWidget5_1(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 5 file 1: a widget controller.\nexport function setupWidget5_1(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-05/widget-2.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-05/widget-2.ts bb/src/features/module-05/widget-2.ts\nindex 21903061..0097ecbd 100644\n--- aa/src/features/module-05/widget-2.ts\n+++ bb/src/features/module-05/widget-2.ts\n@@ -8,10 +8,11 @@ export function setupWidget5_2(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 5 file 2: a widget controller.\nexport function setupWidget5_2(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 5 file 2: a widget controller.\nexport function setupWidget5_2(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-05/widget-3.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-05/widget-3.ts bb/src/features/module-05/widget-3.ts\nindex 58a2f191..8dc831a3 100644\n--- aa/src/features/module-05/widget-3.ts\n+++ bb/src/features/module-05/widget-3.ts\n@@ -8,10 +8,11 @@ export function setupWidget5_3(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 5 file 3: a widget controller.\nexport function setupWidget5_3(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 5 file 3: a widget controller.\nexport function setupWidget5_3(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-05/widget-4.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-05/widget-4.ts bb/src/features/module-05/widget-4.ts\nindex acd9f592..e7ac948d 100644\n--- aa/src/features/module-05/widget-4.ts\n+++ bb/src/features/module-05/widget-4.ts\n@@ -8,10 +8,11 @@ export function setupWidget5_4(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 5 file 4: a widget controller.\nexport function setupWidget5_4(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 5 file 4: a widget controller.\nexport function setupWidget5_4(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-06/widget-0.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-06/widget-0.ts bb/src/features/module-06/widget-0.ts\nindex e39ff00e..1dde5f67 100644\n--- aa/src/features/module-06/widget-0.ts\n+++ bb/src/features/module-06/widget-0.ts\n@@ -8,10 +8,11 @@ export function setupWidget6_0(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 6 file 0: a widget controller.\nexport function setupWidget6_0(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 6 file 0: a widget controller.\nexport function setupWidget6_0(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-06/widget-1.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-06/widget-1.ts bb/src/features/module-06/widget-1.ts\nindex 7e72b037..606d6506 100644\n--- aa/src/features/module-06/widget-1.ts\n+++ bb/src/features/module-06/widget-1.ts\n@@ -8,10 +8,11 @@ export function setupWidget6_1(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 6 file 1: a widget controller.\nexport function setupWidget6_1(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 6 file 1: a widget controller.\nexport function setupWidget6_1(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-06/widget-2.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-06/widget-2.ts bb/src/features/module-06/widget-2.ts\nindex e68d7f9b..1a22b131 100644\n--- aa/src/features/module-06/widget-2.ts\n+++ bb/src/features/module-06/widget-2.ts\n@@ -8,10 +8,11 @@ export function setupWidget6_2(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 6 file 2: a widget controller.\nexport function setupWidget6_2(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 6 file 2: a widget controller.\nexport function setupWidget6_2(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-06/widget-3.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-06/widget-3.ts bb/src/features/module-06/widget-3.ts\nindex 0f15ee0d..54fb923d 100644\n--- aa/src/features/module-06/widget-3.ts\n+++ bb/src/features/module-06/widget-3.ts\n@@ -8,10 +8,11 @@ export function setupWidget6_3(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 6 file 3: a widget controller.\nexport function setupWidget6_3(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 6 file 3: a widget controller.\nexport function setupWidget6_3(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-06/widget-4.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-06/widget-4.ts bb/src/features/module-06/widget-4.ts\nindex 854af603..692fe556 100644\n--- aa/src/features/module-06/widget-4.ts\n+++ bb/src/features/module-06/widget-4.ts\n@@ -8,10 +8,11 @@ export function setupWidget6_4(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 6 file 4: a widget controller.\nexport function setupWidget6_4(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\t// keep the widget 104 in sync with downstream consumers\n\tconst widget105 = createWidget(widget, 105);\n\tstate = widget.reduce(state, widget106);\n\tlogger.debug('widget 107', widget107);\n\tif (widget.active) state.widgetCount += 108;\n\t// keep the widget 109 in sync with downstream consumers\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 6 file 4: a widget controller.\nexport function setupWidget6_4(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\t// keep the widget 104 in sync with downstream consumers\n\tconst widget105 = createWidget(widget, 105);\n\tstate = widget.reduce(state, widget106);\n\tlogger.debug('widget 107', widget107);\n\tif (widget.active) state.widgetCount += 108;\n\t// keep the widget 109 in sync with downstream consumers\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-07/widget-0.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-07/widget-0.ts bb/src/features/module-07/widget-0.ts\nindex df4d1c59..eca62529 100644\n--- aa/src/features/module-07/widget-0.ts\n+++ bb/src/features/module-07/widget-0.ts\n@@ -8,10 +8,11 @@ export function setupWidget7_0(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 7 file 0: a widget controller.\nexport function setupWidget7_0(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 7 file 0: a widget controller.\nexport function setupWidget7_0(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-07/widget-1.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-07/widget-1.ts bb/src/features/module-07/widget-1.ts\nindex 7b477340..62d2ceee 100644\n--- aa/src/features/module-07/widget-1.ts\n+++ bb/src/features/module-07/widget-1.ts\n@@ -8,10 +8,11 @@ export function setupWidget7_1(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 7 file 1: a widget controller.\nexport function setupWidget7_1(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 7 file 1: a widget controller.\nexport function setupWidget7_1(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-07/widget-2.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-07/widget-2.ts bb/src/features/module-07/widget-2.ts\nindex 6ba4e887..bab140ab 100644\n--- aa/src/features/module-07/widget-2.ts\n+++ bb/src/features/module-07/widget-2.ts\n@@ -8,10 +8,11 @@ export function setupWidget7_2(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 7 file 2: a widget controller.\nexport function setupWidget7_2(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 7 file 2: a widget controller.\nexport function setupWidget7_2(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-07/widget-3.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-07/widget-3.ts bb/src/features/module-07/widget-3.ts\nindex 93e68e40..21428b7e 100644\n--- aa/src/features/module-07/widget-3.ts\n+++ bb/src/features/module-07/widget-3.ts\n@@ -8,10 +8,11 @@ export function setupWidget7_3(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 7 file 3: a widget controller.\nexport function setupWidget7_3(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 7 file 3: a widget controller.\nexport function setupWidget7_3(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-07/widget-4.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-07/widget-4.ts bb/src/features/module-07/widget-4.ts\nindex c52eaed6..2604f7e8 100644\n--- aa/src/features/module-07/widget-4.ts\n+++ bb/src/features/module-07/widget-4.ts\n@@ -8,10 +8,11 @@ export function setupWidget7_4(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 7 file 4: a widget controller.\nexport function setupWidget7_4(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\t// keep the widget 104 in sync with downstream consumers\n\tconst widget105 = createWidget(widget, 105);\n\tstate = widget.reduce(state, widget106);\n\tlogger.debug('widget 107', widget107);\n\tif (widget.active) state.widgetCount += 108;\n\t// keep the widget 109 in sync with downstream consumers\n\tconst widget110 = createWidget(widget, 110);\n\tstate = widget.reduce(state, widget111);\n\tlogger.debug('widget 112', widget112);\n\tif (widget.active) state.widgetCount += 113;\n\t// keep the widget 114 in sync with downstream consumers\n\tconst widget115 = createWidget(widget, 115);\n\tstate = widget.reduce(state, widget116);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 7 file 4: a widget controller.\nexport function setupWidget7_4(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\t// keep the widget 104 in sync with downstream consumers\n\tconst widget105 = createWidget(widget, 105);\n\tstate = widget.reduce(state, widget106);\n\tlogger.debug('widget 107', widget107);\n\tif (widget.active) state.widgetCount += 108;\n\t// keep the widget 109 in sync with downstream consumers\n\tconst widget110 = createWidget(widget, 110);\n\tstate = widget.reduce(state, widget111);\n\tlogger.debug('widget 112', widget112);\n\tif (widget.active) state.widgetCount += 113;\n\t// keep the widget 114 in sync with downstream consumers\n\tconst widget115 = createWidget(widget, 115);\n\tstate = widget.reduce(state, widget116);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-08/widget-0.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-08/widget-0.ts bb/src/features/module-08/widget-0.ts\nindex 331d1c11..4a69976d 100644\n--- aa/src/features/module-08/widget-0.ts\n+++ bb/src/features/module-08/widget-0.ts\n@@ -8,10 +8,11 @@ export function setupWidget8_0(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 8 file 0: a widget controller.\nexport function setupWidget8_0(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 8 file 0: a widget controller.\nexport function setupWidget8_0(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-08/widget-1.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-08/widget-1.ts bb/src/features/module-08/widget-1.ts\nindex 2a9bb254..9e6c10ca 100644\n--- aa/src/features/module-08/widget-1.ts\n+++ bb/src/features/module-08/widget-1.ts\n@@ -8,10 +8,11 @@ export function setupWidget8_1(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 8 file 1: a widget controller.\nexport function setupWidget8_1(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 8 file 1: a widget controller.\nexport function setupWidget8_1(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-08/widget-2.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-08/widget-2.ts bb/src/features/module-08/widget-2.ts\nindex 4b6dbc87..de402095 100644\n--- aa/src/features/module-08/widget-2.ts\n+++ bb/src/features/module-08/widget-2.ts\n@@ -8,10 +8,11 @@ export function setupWidget8_2(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 8 file 2: a widget controller.\nexport function setupWidget8_2(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 8 file 2: a widget controller.\nexport function setupWidget8_2(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-08/widget-3.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-08/widget-3.ts bb/src/features/module-08/widget-3.ts\nindex 03ab61ed..ce62ea6f 100644\n--- aa/src/features/module-08/widget-3.ts\n+++ bb/src/features/module-08/widget-3.ts\n@@ -8,10 +8,11 @@ export function setupWidget8_3(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 8 file 3: a widget controller.\nexport function setupWidget8_3(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\t// keep the widget 104 in sync with downstream consumers\n\tconst widget105 = createWidget(widget, 105);\n\tstate = widget.reduce(state, widget106);\n\tlogger.debug('widget 107', widget107);\n\tif (widget.active) state.widgetCount += 108;\n\t// keep the widget 109 in sync with downstream consumers\n\tconst widget110 = createWidget(widget, 110);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 8 file 3: a widget controller.\nexport function setupWidget8_3(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\t// keep the widget 104 in sync with downstream consumers\n\tconst widget105 = createWidget(widget, 105);\n\tstate = widget.reduce(state, widget106);\n\tlogger.debug('widget 107', widget107);\n\tif (widget.active) state.widgetCount += 108;\n\t// keep the widget 109 in sync with downstream consumers\n\tconst widget110 = createWidget(widget, 110);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-08/widget-4.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-08/widget-4.ts bb/src/features/module-08/widget-4.ts\nindex 864200c4..2cabac8e 100644\n--- aa/src/features/module-08/widget-4.ts\n+++ bb/src/features/module-08/widget-4.ts\n@@ -8,10 +8,11 @@ export function setupWidget8_4(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 8 file 4: a widget controller.\nexport function setupWidget8_4(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\t// keep the widget 104 in sync with downstream consumers\n\tconst widget105 = createWidget(widget, 105);\n\tstate = widget.reduce(state, widget106);\n\tlogger.debug('widget 107', widget107);\n\tif (widget.active) state.widgetCount += 108;\n\t// keep the widget 109 in sync with downstream consumers\n\tconst widget110 = createWidget(widget, 110);\n\tstate = widget.reduce(state, widget111);\n\tlogger.debug('widget 112', widget112);\n\tif (widget.active) state.widgetCount += 113;\n\t// keep the widget 114 in sync with downstream consumers\n\tconst widget115 = createWidget(widget, 115);\n\tstate = widget.reduce(state, widget116);\n\tlogger.debug('widget 117', widget117);\n\tif (widget.active) state.widgetCount += 118;\n\t// keep the widget 119 in sync with downstream consumers\n\tconst widget120 = createWidget(widget, 120);\n\tstate = widget.reduce(state, widget121);\n\tlogger.debug('widget 122', widget122);\n\tif (widget.active) state.widgetCount += 123;\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 8 file 4: a widget controller.\nexport function setupWidget8_4(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\t// keep the widget 104 in sync with downstream consumers\n\tconst widget105 = createWidget(widget, 105);\n\tstate = widget.reduce(state, widget106);\n\tlogger.debug('widget 107', widget107);\n\tif (widget.active) state.widgetCount += 108;\n\t// keep the widget 109 in sync with downstream consumers\n\tconst widget110 = createWidget(widget, 110);\n\tstate = widget.reduce(state, widget111);\n\tlogger.debug('widget 112', widget112);\n\tif (widget.active) state.widgetCount += 113;\n\t// keep the widget 114 in sync with downstream consumers\n\tconst widget115 = createWidget(widget, 115);\n\tstate = widget.reduce(state, widget116);\n\tlogger.debug('widget 117', widget117);\n\tif (widget.active) state.widgetCount += 118;\n\t// keep the widget 119 in sync with downstream consumers\n\tconst widget120 = createWidget(widget, 120);\n\tstate = widget.reduce(state, widget121);\n\tlogger.debug('widget 122', widget122);\n\tif (widget.active) state.widgetCount += 123;\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-09/widget-0.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-09/widget-0.ts bb/src/features/module-09/widget-0.ts\nindex 7b6aaaf7..1999e032 100644\n--- aa/src/features/module-09/widget-0.ts\n+++ bb/src/features/module-09/widget-0.ts\n@@ -8,10 +8,11 @@ export function setupWidget9_0(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 9 file 0: a widget controller.\nexport function setupWidget9_0(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 9 file 0: a widget controller.\nexport function setupWidget9_0(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-09/widget-1.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-09/widget-1.ts bb/src/features/module-09/widget-1.ts\nindex fffd75bb..86dfafb5 100644\n--- aa/src/features/module-09/widget-1.ts\n+++ bb/src/features/module-09/widget-1.ts\n@@ -8,10 +8,11 @@ export function setupWidget9_1(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 9 file 1: a widget controller.\nexport function setupWidget9_1(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 9 file 1: a widget controller.\nexport function setupWidget9_1(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-09/widget-2.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-09/widget-2.ts bb/src/features/module-09/widget-2.ts\nindex 03c4642f..5c1f7325 100644\n--- aa/src/features/module-09/widget-2.ts\n+++ bb/src/features/module-09/widget-2.ts\n@@ -8,10 +8,11 @@ export function setupWidget9_2(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 9 file 2: a widget controller.\nexport function setupWidget9_2(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\t// keep the widget 104 in sync with downstream consumers\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 9 file 2: a widget controller.\nexport function setupWidget9_2(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\t// keep the widget 104 in sync with downstream consumers\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-09/widget-3.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-09/widget-3.ts bb/src/features/module-09/widget-3.ts\nindex e65f50d8..a8fda90e 100644\n--- aa/src/features/module-09/widget-3.ts\n+++ bb/src/features/module-09/widget-3.ts\n@@ -8,10 +8,11 @@ export function setupWidget9_3(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 9 file 3: a widget controller.\nexport function setupWidget9_3(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\t// keep the widget 104 in sync with downstream consumers\n\tconst widget105 = createWidget(widget, 105);\n\tstate = widget.reduce(state, widget106);\n\tlogger.debug('widget 107', widget107);\n\tif (widget.active) state.widgetCount += 108;\n\t// keep the widget 109 in sync with downstream consumers\n\tconst widget110 = createWidget(widget, 110);\n\tstate = widget.reduce(state, widget111);\n\tlogger.debug('widget 112', widget112);\n\tif (widget.active) state.widgetCount += 113;\n\t// keep the widget 114 in sync with downstream consumers\n\tconst widget115 = createWidget(widget, 115);\n\tstate = widget.reduce(state, widget116);\n\tlogger.debug('widget 117', widget117);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 9 file 3: a widget controller.\nexport function setupWidget9_3(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\t// keep the widget 104 in sync with downstream consumers\n\tconst widget105 = createWidget(widget, 105);\n\tstate = widget.reduce(state, widget106);\n\tlogger.debug('widget 107', widget107);\n\tif (widget.active) state.widgetCount += 108;\n\t// keep the widget 109 in sync with downstream consumers\n\tconst widget110 = createWidget(widget, 110);\n\tstate = widget.reduce(state, widget111);\n\tlogger.debug('widget 112', widget112);\n\tif (widget.active) state.widgetCount += 113;\n\t// keep the widget 114 in sync with downstream consumers\n\tconst widget115 = createWidget(widget, 115);\n\tstate = widget.reduce(state, widget116);\n\tlogger.debug('widget 117', widget117);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-09/widget-4.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-09/widget-4.ts bb/src/features/module-09/widget-4.ts\nindex 9973b889..0cbf9530 100644\n--- aa/src/features/module-09/widget-4.ts\n+++ bb/src/features/module-09/widget-4.ts\n@@ -8,10 +8,11 @@ export function setupWidget9_4(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 9 file 4: a widget controller.\nexport function setupWidget9_4(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\t// keep the widget 104 in sync with downstream consumers\n\tconst widget105 = createWidget(widget, 105);\n\tstate = widget.reduce(state, widget106);\n\tlogger.debug('widget 107', widget107);\n\tif (widget.active) state.widgetCount += 108;\n\t// keep the widget 109 in sync with downstream consumers\n\tconst widget110 = createWidget(widget, 110);\n\tstate = widget.reduce(state, widget111);\n\tlogger.debug('widget 112', widget112);\n\tif (widget.active) state.widgetCount += 113;\n\t// keep the widget 114 in sync with downstream consumers\n\tconst widget115 = createWidget(widget, 115);\n\tstate = widget.reduce(state, widget116);\n\tlogger.debug('widget 117', widget117);\n\tif (widget.active) state.widgetCount += 118;\n\t// keep the widget 119 in sync with downstream consumers\n\tconst widget120 = createWidget(widget, 120);\n\tstate = widget.reduce(state, widget121);\n\tlogger.debug('widget 122', widget122);\n\tif (widget.active) state.widgetCount += 123;\n\t// keep the widget 124 in sync with downstream consumers\n\tconst widget125 = createWidget(widget, 125);\n\tstate = widget.reduce(state, widget126);\n\tlogger.debug('widget 127', widget127);\n\tif (widget.active) state.widgetCount += 128;\n\t// keep the widget 129 in sync with downstream consumers\n\tconst widget130 = createWidget(widget, 130);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 9 file 4: a widget controller.\nexport function setupWidget9_4(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\t// keep the widget 104 in sync with downstream consumers\n\tconst widget105 = createWidget(widget, 105);\n\tstate = widget.reduce(state, widget106);\n\tlogger.debug('widget 107', widget107);\n\tif (widget.active) state.widgetCount += 108;\n\t// keep the widget 109 in sync with downstream consumers\n\tconst widget110 = createWidget(widget, 110);\n\tstate = widget.reduce(state, widget111);\n\tlogger.debug('widget 112', widget112);\n\tif (widget.active) state.widgetCount += 113;\n\t// keep the widget 114 in sync with downstream consumers\n\tconst widget115 = createWidget(widget, 115);\n\tstate = widget.reduce(state, widget116);\n\tlogger.debug('widget 117', widget117);\n\tif (widget.active) state.widgetCount += 118;\n\t// keep the widget 119 in sync with downstream consumers\n\tconst widget120 = createWidget(widget, 120);\n\tstate = widget.reduce(state, widget121);\n\tlogger.debug('widget 122', widget122);\n\tif (widget.active) state.widgetCount += 123;\n\t// keep the widget 124 in sync with downstream consumers\n\tconst widget125 = createWidget(widget, 125);\n\tstate = widget.reduce(state, widget126);\n\tlogger.debug('widget 127', widget127);\n\tif (widget.active) state.widgetCount += 128;\n\t// keep the widget 129 in sync with downstream consumers\n\tconst widget130 = createWidget(widget, 130);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-10/widget-0.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-10/widget-0.ts bb/src/features/module-10/widget-0.ts\nindex add1bee2..7ea5a90b 100644\n--- aa/src/features/module-10/widget-0.ts\n+++ bb/src/features/module-10/widget-0.ts\n@@ -8,10 +8,11 @@ export function setupWidget10_0(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 10 file 0: a widget controller.\nexport function setupWidget10_0(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 10 file 0: a widget controller.\nexport function setupWidget10_0(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-10/widget-1.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-10/widget-1.ts bb/src/features/module-10/widget-1.ts\nindex a90c0f2d..b5f95b78 100644\n--- aa/src/features/module-10/widget-1.ts\n+++ bb/src/features/module-10/widget-1.ts\n@@ -8,10 +8,11 @@ export function setupWidget10_1(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 10 file 1: a widget controller.\nexport function setupWidget10_1(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 10 file 1: a widget controller.\nexport function setupWidget10_1(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-10/widget-2.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-10/widget-2.ts bb/src/features/module-10/widget-2.ts\nindex 978e6063..4201f1b8 100644\n--- aa/src/features/module-10/widget-2.ts\n+++ bb/src/features/module-10/widget-2.ts\n@@ -8,10 +8,11 @@ export function setupWidget10_2(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 10 file 2: a widget controller.\nexport function setupWidget10_2(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\t// keep the widget 104 in sync with downstream consumers\n\tconst widget105 = createWidget(widget, 105);\n\tstate = widget.reduce(state, widget106);\n\tlogger.debug('widget 107', widget107);\n\tif (widget.active) state.widgetCount += 108;\n\t// keep the widget 109 in sync with downstream consumers\n\tconst widget110 = createWidget(widget, 110);\n\tstate = widget.reduce(state, widget111);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 10 file 2: a widget controller.\nexport function setupWidget10_2(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\t// keep the widget 104 in sync with downstream consumers\n\tconst widget105 = createWidget(widget, 105);\n\tstate = widget.reduce(state, widget106);\n\tlogger.debug('widget 107', widget107);\n\tif (widget.active) state.widgetCount += 108;\n\t// keep the widget 109 in sync with downstream consumers\n\tconst widget110 = createWidget(widget, 110);\n\tstate = widget.reduce(state, widget111);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-10/widget-3.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-10/widget-3.ts bb/src/features/module-10/widget-3.ts\nindex c787c8a3..45204180 100644\n--- aa/src/features/module-10/widget-3.ts\n+++ bb/src/features/module-10/widget-3.ts\n@@ -8,10 +8,11 @@ export function setupWidget10_3(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 10 file 3: a widget controller.\nexport function setupWidget10_3(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\t// keep the widget 104 in sync with downstream consumers\n\tconst widget105 = createWidget(widget, 105);\n\tstate = widget.reduce(state, widget106);\n\tlogger.debug('widget 107', widget107);\n\tif (widget.active) state.widgetCount += 108;\n\t// keep the widget 109 in sync with downstream consumers\n\tconst widget110 = createWidget(widget, 110);\n\tstate = widget.reduce(state, widget111);\n\tlogger.debug('widget 112', widget112);\n\tif (widget.active) state.widgetCount += 113;\n\t// keep the widget 114 in sync with downstream consumers\n\tconst widget115 = createWidget(widget, 115);\n\tstate = widget.reduce(state, widget116);\n\tlogger.debug('widget 117', widget117);\n\tif (widget.active) state.widgetCount += 118;\n\t// keep the widget 119 in sync with downstream consumers\n\tconst widget120 = createWidget(widget, 120);\n\tstate = widget.reduce(state, widget121);\n\tlogger.debug('widget 122', widget122);\n\tif (widget.active) state.widgetCount += 123;\n\t// keep the widget 124 in sync with downstream consumers\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 10 file 3: a widget controller.\nexport function setupWidget10_3(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\t// keep the widget 104 in sync with downstream consumers\n\tconst widget105 = createWidget(widget, 105);\n\tstate = widget.reduce(state, widget106);\n\tlogger.debug('widget 107', widget107);\n\tif (widget.active) state.widgetCount += 108;\n\t// keep the widget 109 in sync with downstream consumers\n\tconst widget110 = createWidget(widget, 110);\n\tstate = widget.reduce(state, widget111);\n\tlogger.debug('widget 112', widget112);\n\tif (widget.active) state.widgetCount += 113;\n\t// keep the widget 114 in sync with downstream consumers\n\tconst widget115 = createWidget(widget, 115);\n\tstate = widget.reduce(state, widget116);\n\tlogger.debug('widget 117', widget117);\n\tif (widget.active) state.widgetCount += 118;\n\t// keep the widget 119 in sync with downstream consumers\n\tconst widget120 = createWidget(widget, 120);\n\tstate = widget.reduce(state, widget121);\n\tlogger.debug('widget 122', widget122);\n\tif (widget.active) state.widgetCount += 123;\n\t// keep the widget 124 in sync with downstream consumers\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-10/widget-4.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-10/widget-4.ts bb/src/features/module-10/widget-4.ts\nindex 3d80f1e1..399802cb 100644\n--- aa/src/features/module-10/widget-4.ts\n+++ bb/src/features/module-10/widget-4.ts\n@@ -8,10 +8,11 @@ export function setupWidget10_4(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 10 file 4: a widget controller.\nexport function setupWidget10_4(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\t// keep the widget 104 in sync with downstream consumers\n\tconst widget105 = createWidget(widget, 105);\n\tstate = widget.reduce(state, widget106);\n\tlogger.debug('widget 107', widget107);\n\tif (widget.active) state.widgetCount += 108;\n\t// keep the widget 109 in sync with downstream consumers\n\tconst widget110 = createWidget(widget, 110);\n\tstate = widget.reduce(state, widget111);\n\tlogger.debug('widget 112', widget112);\n\tif (widget.active) state.widgetCount += 113;\n\t// keep the widget 114 in sync with downstream consumers\n\tconst widget115 = createWidget(widget, 115);\n\tstate = widget.reduce(state, widget116);\n\tlogger.debug('widget 117', widget117);\n\tif (widget.active) state.widgetCount += 118;\n\t// keep the widget 119 in sync with downstream consumers\n\tconst widget120 = createWidget(widget, 120);\n\tstate = widget.reduce(state, widget121);\n\tlogger.debug('widget 122', widget122);\n\tif (widget.active) state.widgetCount += 123;\n\t// keep the widget 124 in sync with downstream consumers\n\tconst widget125 = createWidget(widget, 125);\n\tstate = widget.reduce(state, widget126);\n\tlogger.debug('widget 127', widget127);\n\tif (widget.active) state.widgetCount += 128;\n\t// keep the widget 129 in sync with downstream consumers\n\tconst widget130 = createWidget(widget, 130);\n\tstate = widget.reduce(state, widget131);\n\tlogger.debug('widget 132', widget132);\n\tif (widget.active) state.widgetCount += 133;\n\t// keep the widget 134 in sync with downstream consumers\n\tconst widget135 = createWidget(widget, 135);\n\tstate = widget.reduce(state, widget136);\n\tlogger.debug('widget 137', widget137);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 10 file 4: a widget controller.\nexport function setupWidget10_4(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\t// keep the widget 104 in sync with downstream consumers\n\tconst widget105 = createWidget(widget, 105);\n\tstate = widget.reduce(state, widget106);\n\tlogger.debug('widget 107', widget107);\n\tif (widget.active) state.widgetCount += 108;\n\t// keep the widget 109 in sync with downstream consumers\n\tconst widget110 = createWidget(widget, 110);\n\tstate = widget.reduce(state, widget111);\n\tlogger.debug('widget 112', widget112);\n\tif (widget.active) state.widgetCount += 113;\n\t// keep the widget 114 in sync with downstream consumers\n\tconst widget115 = createWidget(widget, 115);\n\tstate = widget.reduce(state, widget116);\n\tlogger.debug('widget 117', widget117);\n\tif (widget.active) state.widgetCount += 118;\n\t// keep the widget 119 in sync with downstream consumers\n\tconst widget120 = createWidget(widget, 120);\n\tstate = widget.reduce(state, widget121);\n\tlogger.debug('widget 122', widget122);\n\tif (widget.active) state.widgetCount += 123;\n\t// keep the widget 124 in sync with downstream consumers\n\tconst widget125 = createWidget(widget, 125);\n\tstate = widget.reduce(state, widget126);\n\tlogger.debug('widget 127', widget127);\n\tif (widget.active) state.widgetCount += 128;\n\t// keep the widget 129 in sync with downstream consumers\n\tconst widget130 = createWidget(widget, 130);\n\tstate = widget.reduce(state, widget131);\n\tlogger.debug('widget 132', widget132);\n\tif (widget.active) state.widgetCount += 133;\n\t// keep the widget 134 in sync with downstream consumers\n\tconst widget135 = createWidget(widget, 135);\n\tstate = widget.reduce(state, widget136);\n\tlogger.debug('widget 137', widget137);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-11/widget-0.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-11/widget-0.ts bb/src/features/module-11/widget-0.ts\nindex 9bbf00fc..788601fa 100644\n--- aa/src/features/module-11/widget-0.ts\n+++ bb/src/features/module-11/widget-0.ts\n@@ -8,10 +8,11 @@ export function setupWidget11_0(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 11 file 0: a widget controller.\nexport function setupWidget11_0(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 11 file 0: a widget controller.\nexport function setupWidget11_0(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-11/widget-1.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-11/widget-1.ts bb/src/features/module-11/widget-1.ts\nindex a9e60c1c..4eeed612 100644\n--- aa/src/features/module-11/widget-1.ts\n+++ bb/src/features/module-11/widget-1.ts\n@@ -8,10 +8,11 @@ export function setupWidget11_1(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 11 file 1: a widget controller.\nexport function setupWidget11_1(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\t// keep the widget 104 in sync with downstream consumers\n\tconst widget105 = createWidget(widget, 105);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 11 file 1: a widget controller.\nexport function setupWidget11_1(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\t// keep the widget 104 in sync with downstream consumers\n\tconst widget105 = createWidget(widget, 105);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-11/widget-2.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-11/widget-2.ts bb/src/features/module-11/widget-2.ts\nindex d7c85fa3..03bdd5e7 100644\n--- aa/src/features/module-11/widget-2.ts\n+++ bb/src/features/module-11/widget-2.ts\n@@ -8,10 +8,11 @@ export function setupWidget11_2(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 11 file 2: a widget controller.\nexport function setupWidget11_2(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\t// keep the widget 104 in sync with downstream consumers\n\tconst widget105 = createWidget(widget, 105);\n\tstate = widget.reduce(state, widget106);\n\tlogger.debug('widget 107', widget107);\n\tif (widget.active) state.widgetCount += 108;\n\t// keep the widget 109 in sync with downstream consumers\n\tconst widget110 = createWidget(widget, 110);\n\tstate = widget.reduce(state, widget111);\n\tlogger.debug('widget 112', widget112);\n\tif (widget.active) state.widgetCount += 113;\n\t// keep the widget 114 in sync with downstream consumers\n\tconst widget115 = createWidget(widget, 115);\n\tstate = widget.reduce(state, widget116);\n\tlogger.debug('widget 117', widget117);\n\tif (widget.active) state.widgetCount += 118;\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 11 file 2: a widget controller.\nexport function setupWidget11_2(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\t// keep the widget 104 in sync with downstream consumers\n\tconst widget105 = createWidget(widget, 105);\n\tstate = widget.reduce(state, widget106);\n\tlogger.debug('widget 107', widget107);\n\tif (widget.active) state.widgetCount += 108;\n\t// keep the widget 109 in sync with downstream consumers\n\tconst widget110 = createWidget(widget, 110);\n\tstate = widget.reduce(state, widget111);\n\tlogger.debug('widget 112', widget112);\n\tif (widget.active) state.widgetCount += 113;\n\t// keep the widget 114 in sync with downstream consumers\n\tconst widget115 = createWidget(widget, 115);\n\tstate = widget.reduce(state, widget116);\n\tlogger.debug('widget 117', widget117);\n\tif (widget.active) state.widgetCount += 118;\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-11/widget-3.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-11/widget-3.ts bb/src/features/module-11/widget-3.ts\nindex 9ae2a40c..5fc7599d 100644\n--- aa/src/features/module-11/widget-3.ts\n+++ bb/src/features/module-11/widget-3.ts\n@@ -8,10 +8,11 @@ export function setupWidget11_3(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 11 file 3: a widget controller.\nexport function setupWidget11_3(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\t// keep the widget 104 in sync with downstream consumers\n\tconst widget105 = createWidget(widget, 105);\n\tstate = widget.reduce(state, widget106);\n\tlogger.debug('widget 107', widget107);\n\tif (widget.active) state.widgetCount += 108;\n\t// keep the widget 109 in sync with downstream consumers\n\tconst widget110 = createWidget(widget, 110);\n\tstate = widget.reduce(state, widget111);\n\tlogger.debug('widget 112', widget112);\n\tif (widget.active) state.widgetCount += 113;\n\t// keep the widget 114 in sync with downstream consumers\n\tconst widget115 = createWidget(widget, 115);\n\tstate = widget.reduce(state, widget116);\n\tlogger.debug('widget 117', widget117);\n\tif (widget.active) state.widgetCount += 118;\n\t// keep the widget 119 in sync with downstream consumers\n\tconst widget120 = createWidget(widget, 120);\n\tstate = widget.reduce(state, widget121);\n\tlogger.debug('widget 122', widget122);\n\tif (widget.active) state.widgetCount += 123;\n\t// keep the widget 124 in sync with downstream consumers\n\tconst widget125 = createWidget(widget, 125);\n\tstate = widget.reduce(state, widget126);\n\tlogger.debug('widget 127', widget127);\n\tif (widget.active) state.widgetCount += 128;\n\t// keep the widget 129 in sync with downstream consumers\n\tconst widget130 = createWidget(widget, 130);\n\tstate = widget.reduce(state, widget131);\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 11 file 3: a widget controller.\nexport function setupWidget11_3(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\t// keep the widget 104 in sync with downstream consumers\n\tconst widget105 = createWidget(widget, 105);\n\tstate = widget.reduce(state, widget106);\n\tlogger.debug('widget 107', widget107);\n\tif (widget.active) state.widgetCount += 108;\n\t// keep the widget 109 in sync with downstream consumers\n\tconst widget110 = createWidget(widget, 110);\n\tstate = widget.reduce(state, widget111);\n\tlogger.debug('widget 112', widget112);\n\tif (widget.active) state.widgetCount += 113;\n\t// keep the widget 114 in sync with downstream consumers\n\tconst widget115 = createWidget(widget, 115);\n\tstate = widget.reduce(state, widget116);\n\tlogger.debug('widget 117', widget117);\n\tif (widget.active) state.widgetCount += 118;\n\t// keep the widget 119 in sync with downstream consumers\n\tconst widget120 = createWidget(widget, 120);\n\tstate = widget.reduce(state, widget121);\n\tlogger.debug('widget 122', widget122);\n\tif (widget.active) state.widgetCount += 123;\n\t// keep the widget 124 in sync with downstream consumers\n\tconst widget125 = createWidget(widget, 125);\n\tstate = widget.reduce(state, widget126);\n\tlogger.debug('widget 127', widget127);\n\tif (widget.active) state.widgetCount += 128;\n\t// keep the widget 129 in sync with downstream consumers\n\tconst widget130 = createWidget(widget, 130);\n\tstate = widget.reduce(state, widget131);\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		},
		{
			"file": {
				"path": "src/features/module-11/widget-4.ts",
				"status": "modified",
				"additions": 2,
				"deletions": 1,
				"isBinary": false
			},
			"patch": "diff --git aa/src/features/module-11/widget-4.ts bb/src/features/module-11/widget-4.ts\nindex 9086f5d7..0e4a842c 100644\n--- aa/src/features/module-11/widget-4.ts\n+++ bb/src/features/module-11/widget-4.ts\n@@ -8,10 +8,11 @@ export function setupWidget11_4(props: WidgetProps) {\n \tlet state: WidgetState = widget.initialState;\n \tconst widget0 = createWidget(widget, 0);\n \tstate = widget.reduce(state, widget1);\n+\tlogger.debug('widget 2', widget2);\n \tif (widget.active) state.widgetCount += 3;\n \t// keep the widget 4 in sync with downstream consumers\n \tconst widget5 = createWidget(widget, 5);\n-\tstate = widget.reduce(state, oldwidget6);\n+\tstate = widget.reduce(state, widget6);\n \tlogger.debug('widget 7', widget7);\n \tif (widget.active) state.widgetCount += 8;\n \t// keep the widget 9 in sync with downstream consumers\n",
			"oldContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 11 file 4: a widget controller.\nexport function setupWidget11_4(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, oldwidget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\t// keep the widget 104 in sync with downstream consumers\n\tconst widget105 = createWidget(widget, 105);\n\tstate = widget.reduce(state, widget106);\n\tlogger.debug('widget 107', widget107);\n\tif (widget.active) state.widgetCount += 108;\n\t// keep the widget 109 in sync with downstream consumers\n\tconst widget110 = createWidget(widget, 110);\n\tstate = widget.reduce(state, widget111);\n\tlogger.debug('widget 112', widget112);\n\tif (widget.active) state.widgetCount += 113;\n\t// keep the widget 114 in sync with downstream consumers\n\tconst widget115 = createWidget(widget, 115);\n\tstate = widget.reduce(state, widget116);\n\tlogger.debug('widget 117', widget117);\n\tif (widget.active) state.widgetCount += 118;\n\t// keep the widget 119 in sync with downstream consumers\n\tconst widget120 = createWidget(widget, 120);\n\tstate = widget.reduce(state, widget121);\n\tlogger.debug('widget 122', widget122);\n\tif (widget.active) state.widgetCount += 123;\n\t// keep the widget 124 in sync with downstream consumers\n\tconst widget125 = createWidget(widget, 125);\n\tstate = widget.reduce(state, widget126);\n\tlogger.debug('widget 127', widget127);\n\tif (widget.active) state.widgetCount += 128;\n\t// keep the widget 129 in sync with downstream consumers\n\tconst widget130 = createWidget(widget, 130);\n\tstate = widget.reduce(state, widget131);\n\tlogger.debug('widget 132', widget132);\n\tif (widget.active) state.widgetCount += 133;\n\t// keep the widget 134 in sync with downstream consumers\n\tconst widget135 = createWidget(widget, 135);\n\tstate = widget.reduce(state, widget136);\n\tlogger.debug('widget 137', widget137);\n\tif (widget.active) state.widgetCount += 138;\n\t// keep the widget 139 in sync with downstream consumers\n\tconst widget140 = createWidget(widget, 140);\n\tstate = widget.reduce(state, widget141);\n\tlogger.debug('widget 142', widget142);\n\tif (widget.active) state.widgetCount += 143;\n\t// keep the widget 144 in sync with downstream consumers\n\treturn { widget, state };\n}\n\n",
			"newContents": "import { widgetFactory, createWidget } from '$lib/widgets/core';\nimport type { WidgetProps, WidgetState } from '$lib/widgets/types';\nimport { logger } from '$lib/util/log';\n\n// Module 11 file 4: a widget controller.\nexport function setupWidget11_4(props: WidgetProps) {\n\tconst widget = widgetFactory(props);\n\tlet state: WidgetState = widget.initialState;\n\tconst widget0 = createWidget(widget, 0);\n\tstate = widget.reduce(state, widget1);\n\tlogger.debug('widget 2', widget2);\n\tif (widget.active) state.widgetCount += 3;\n\t// keep the widget 4 in sync with downstream consumers\n\tconst widget5 = createWidget(widget, 5);\n\tstate = widget.reduce(state, widget6);\n\tlogger.debug('widget 7', widget7);\n\tif (widget.active) state.widgetCount += 8;\n\t// keep the widget 9 in sync with downstream consumers\n\tconst widget10 = createWidget(widget, 10);\n\tstate = widget.reduce(state, widget11);\n\tlogger.debug('widget 12', widget12);\n\tif (widget.active) state.widgetCount += 13;\n\t// keep the widget 14 in sync with downstream consumers\n\tconst widget15 = createWidget(widget, 15);\n\tstate = widget.reduce(state, widget16);\n\tlogger.debug('widget 17', widget17);\n\tif (widget.active) state.widgetCount += 18;\n\t// keep the widget 19 in sync with downstream consumers\n\tconst widget20 = createWidget(widget, 20);\n\tstate = widget.reduce(state, widget21);\n\tlogger.debug('widget 22', widget22);\n\tif (widget.active) state.widgetCount += 23;\n\t// keep the widget 24 in sync with downstream consumers\n\tconst widget25 = createWidget(widget, 25);\n\tstate = widget.reduce(state, widget26);\n\tlogger.debug('widget 27', widget27);\n\tif (widget.active) state.widgetCount += 28;\n\t// keep the widget 29 in sync with downstream consumers\n\tconst widget30 = createWidget(widget, 30);\n\tstate = widget.reduce(state, widget31);\n\tlogger.debug('widget 32', widget32);\n\tif (widget.active) state.widgetCount += 33;\n\t// keep the widget 34 in sync with downstream consumers\n\tconst widget35 = createWidget(widget, 35);\n\tstate = widget.reduce(state, widget36);\n\tlogger.debug('widget 37', widget37);\n\tif (widget.active) state.widgetCount += 38;\n\t// keep the widget 39 in sync with downstream consumers\n\tconst widget40 = createWidget(widget, 40);\n\tstate = widget.reduce(state, widget41);\n\tlogger.debug('widget 42', widget42);\n\tif (widget.active) state.widgetCount += 43;\n\t// keep the widget 44 in sync with downstream consumers\n\tconst widget45 = createWidget(widget, 45);\n\tstate = widget.reduce(state, widget46);\n\tlogger.debug('widget 47', widget47);\n\tif (widget.active) state.widgetCount += 48;\n\t// keep the widget 49 in sync with downstream consumers\n\tconst widget50 = createWidget(widget, 50);\n\tstate = widget.reduce(state, widget51);\n\tlogger.debug('widget 52', widget52);\n\tif (widget.active) state.widgetCount += 53;\n\t// keep the widget 54 in sync with downstream consumers\n\tconst widget55 = createWidget(widget, 55);\n\tstate = widget.reduce(state, widget56);\n\tlogger.debug('widget 57', widget57);\n\tif (widget.active) state.widgetCount += 58;\n\t// keep the widget 59 in sync with downstream consumers\n\tconst widget60 = createWidget(widget, 60);\n\tstate = widget.reduce(state, widget61);\n\tlogger.debug('widget 62', widget62);\n\tif (widget.active) state.widgetCount += 63;\n\t// keep the widget 64 in sync with downstream consumers\n\tconst widget65 = createWidget(widget, 65);\n\tstate = widget.reduce(state, widget66);\n\tlogger.debug('widget 67', widget67);\n\tif (widget.active) state.widgetCount += 68;\n\t// keep the widget 69 in sync with downstream consumers\n\tconst widget70 = createWidget(widget, 70);\n\tstate = widget.reduce(state, widget71);\n\tlogger.debug('widget 72', widget72);\n\tif (widget.active) state.widgetCount += 73;\n\t// keep the widget 74 in sync with downstream consumers\n\tconst widget75 = createWidget(widget, 75);\n\tstate = widget.reduce(state, widget76);\n\tlogger.debug('widget 77', widget77);\n\tif (widget.active) state.widgetCount += 78;\n\t// keep the widget 79 in sync with downstream consumers\n\tconst widget80 = createWidget(widget, 80);\n\tstate = widget.reduce(state, widget81);\n\tlogger.debug('widget 82', widget82);\n\tif (widget.active) state.widgetCount += 83;\n\t// keep the widget 84 in sync with downstream consumers\n\tconst widget85 = createWidget(widget, 85);\n\tstate = widget.reduce(state, widget86);\n\tlogger.debug('widget 87', widget87);\n\tif (widget.active) state.widgetCount += 88;\n\t// keep the widget 89 in sync with downstream consumers\n\tconst widget90 = createWidget(widget, 90);\n\tstate = widget.reduce(state, widget91);\n\tlogger.debug('widget 92', widget92);\n\tif (widget.active) state.widgetCount += 93;\n\t// keep the widget 94 in sync with downstream consumers\n\tconst widget95 = createWidget(widget, 95);\n\tstate = widget.reduce(state, widget96);\n\tlogger.debug('widget 97', widget97);\n\tif (widget.active) state.widgetCount += 98;\n\t// keep the widget 99 in sync with downstream consumers\n\tconst widget100 = createWidget(widget, 100);\n\tstate = widget.reduce(state, widget101);\n\tlogger.debug('widget 102', widget102);\n\tif (widget.active) state.widgetCount += 103;\n\t// keep the widget 104 in sync with downstream consumers\n\tconst widget105 = createWidget(widget, 105);\n\tstate = widget.reduce(state, widget106);\n\tlogger.debug('widget 107', widget107);\n\tif (widget.active) state.widgetCount += 108;\n\t// keep the widget 109 in sync with downstream consumers\n\tconst widget110 = createWidget(widget, 110);\n\tstate = widget.reduce(state, widget111);\n\tlogger.debug('widget 112', widget112);\n\tif (widget.active) state.widgetCount += 113;\n\t// keep the widget 114 in sync with downstream consumers\n\tconst widget115 = createWidget(widget, 115);\n\tstate = widget.reduce(state, widget116);\n\tlogger.debug('widget 117', widget117);\n\tif (widget.active) state.widgetCount += 118;\n\t// keep the widget 119 in sync with downstream consumers\n\tconst widget120 = createWidget(widget, 120);\n\tstate = widget.reduce(state, widget121);\n\tlogger.debug('widget 122', widget122);\n\tif (widget.active) state.widgetCount += 123;\n\t// keep the widget 124 in sync with downstream consumers\n\tconst widget125 = createWidget(widget, 125);\n\tstate = widget.reduce(state, widget126);\n\tlogger.debug('widget 127', widget127);\n\tif (widget.active) state.widgetCount += 128;\n\t// keep the widget 129 in sync with downstream consumers\n\tconst widget130 = createWidget(widget, 130);\n\tstate = widget.reduce(state, widget131);\n\tlogger.debug('widget 132', widget132);\n\tif (widget.active) state.widgetCount += 133;\n\t// keep the widget 134 in sync with downstream consumers\n\tconst widget135 = createWidget(widget, 135);\n\tstate = widget.reduce(state, widget136);\n\tlogger.debug('widget 137', widget137);\n\tif (widget.active) state.widgetCount += 138;\n\t// keep the widget 139 in sync with downstream consumers\n\tconst widget140 = createWidget(widget, 140);\n\tstate = widget.reduce(state, widget141);\n\tlogger.debug('widget 142', widget142);\n\tif (widget.active) state.widgetCount += 143;\n\t// keep the widget 144 in sync with downstream consumers\n\treturn { widget, state };\n}\n\n",
			"truncated": false
		}
	]
}