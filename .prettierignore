# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock

# Build output
**/out/
**/release/
**/dist/
**/.svelte-kit/
apps/docs/build/

# Changeset summaries are generated by the changesets CLI with double-quoted
# frontmatter, which conflicts with our singleQuote rule. Don't gate them.
.changeset/*.md
!.changeset/README.md

# Super-review session manifests are generated by the super-review CLI as
# frozen snapshots; they travel with the PR for review but aren't hand-edited,
# so don't gate them on our formatting rules.
.super-review/

# Standalone Cloudflare Worker(s) — deployed separately with their own toolchain
# (Cloudflare Workers types/globals), outside the pnpm workspace. Not gated by
# the app's prettier/eslint config.
infra/
