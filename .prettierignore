# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock

# Build output
**/out/
**/release/
**/dist/
**/.svelte-kit/
apps/docs/build/

# Changeset summaries are generated by the changesets CLI with double-quoted
# frontmatter, which conflicts with our singleQuote rule. Don't gate them.
.changeset/*.md
!.changeset/README.md

# super-review session manifests are generated by the CLI (frozen diff
# snapshots); they're not hand-authored, so don't gate them on formatting.
.super-review/
