# electron-builder packages the app's production dependencies out of node_modules.
# pnpm's default symlinked layout trips up that traversal, so use a hoisted
# (npm-style flat) node_modules. This keeps `pnpm package`/CI releases reliable.
node-linker=hoisted
