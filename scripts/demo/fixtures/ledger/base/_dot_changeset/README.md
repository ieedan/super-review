# Changesets

Every user-visible change gets a changeset. Run `pnpm changeset`, pick a bump,
and describe the change the way a release note would.

The files in this folder are consumed and deleted when a release is versioned.
